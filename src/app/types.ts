
export interface Channel {
  _id: string
  channel: string
  metadata: any;
};

export interface Coordinates {
    _id: string
    channel: string
    coords: number[]
    month: string
    ts: string
};